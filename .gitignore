build/
migrations/
node_modules/

# asdf
.tool-versions

# IDEA
.idea

# decrypted environment configs
.env.staging
.env.alfajores
.env.local
.env

# Foundry
out/
cache/
cache_forge/

# forge deployment records for a local devchain
deployments/local

# Generated devchain fixture (scripts/prepare-devchain.sh)
test/devchain/allocs.json
test/devchain/meta.json

# Compatibility baseline checked out by the CI compatibility job
baseline/

# forge script broadcast logs
broadcast/

# Build output of the frozen Hardhat toolchain under legacy/, in case it is
# ever run for reference. Nothing in CI builds it.
legacy/artifacts/
legacy/cache/
legacy/typechain-types/
legacy/deployments/
legacy/db/

# python bytecode caches (scripts/tests)
__pycache__/
